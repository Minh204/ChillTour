@{
    ViewData["Title"] = "Employee";
}

<section class="section-block">
    <div class="container">
        <div class="section-heading">
            <span class="eyebrow">Employee</span>
            <h2>Trang này đã được chuyển sang khu xử lý đơn</h2>
            <p>Nhấn nút bên dưới để mở dashboard xử lý booking cho nhân viên.</p>
            <a asp-controller="Admin" asp-action="Orders" class="btn btn-primary nav-cta">Mở Employee Dashboard</a>
        </div>
    </div>
</section>
