@model ChillTour.Models.Admin.EmployeeOrdersViewModel
@using ChillTour.Security
@{
    ViewData["Title"] = "Xá»­ lÃ½ Ä‘Æ¡n";
    var canManageBookings = RoleConstants.ManageBookings.Any(User.IsInRole);
    var canManageFinance = RoleConstants.ManageFinance.Any(User.IsInRole);
    var canEditBookingStatus = canManageBookings;
}

<section class="admin-page-stack">
    <div class="admin-page-head">
        <div>
            <span class="admin-panel__label">Operations</span>
            <h2>Xá»­ lÃ½ Ä‘Æ¡n Ä‘áº·t tour</h2>
        </div>
    </div>

    @if (TempData["AdminSuccessMessage"] is string successMessage)
    {
        <div class="auth-alert auth-alert--success">@successMessage</div>
    }
    @if (TempData["AdminErrorMessage"] is string errorMessage)
    {
        <div class="auth-alert auth-alert--error">@errorMessage</div>
    }

    <div class="admin-metrics">
        <div class="admin-stat-card admin-stat-card--orange">
            <span>ÄÆ¡n chá» xá»­ lÃ½</span>
            <strong>@Model.PendingOrders</strong>
            <p>Bao gá»“m Ä‘Æ¡n chá» thanh toÃ¡n hoáº·c chá» Ä‘á»‘i soÃ¡t.</p>
        </div>
        <div class="admin-stat-card admin-stat-card--mint">
            <span>ÄÆ¡n Ä‘Ã£ xÃ¡c nháº­n</span>
            <strong>@Model.ConfirmedOrders</strong>
            <p>Äang váº­n hÃ nh hoáº·c Ä‘Ã£ hoÃ n táº¥t.</p>
        </div>
        <div class="admin-stat-card">
            <span>ÄÆ¡n há»§y/hoÃ n tiá»n</span>
            <strong>@Model.CancelledOrders</strong>
            <p>CÃ¡c Ä‘Æ¡n Ä‘Ã£ Ä‘Ã³ng hoáº·c há»§y.</p>
        </div>
    </div>

    <section class="admin-panel">
        <div class="admin-panel__header">
            <div>
                <span class="admin-panel__label">Bookings</span>
                <h3>Danh sÃ¡ch Ä‘Æ¡n</h3>
            </div>
            <div class="admin-topbar__badge">@Model.TotalItems Ä‘Æ¡n</div>
        </div>

        <form method="get" class="admin-form-grid mb-4">
            <input type="hidden" name="page" value="1" />
            <label>
                Tá»« khÃ³a
                <input type="text" name="searchTerm" value="@Model.Filter.SearchTerm" class="form-control" placeholder="MÃ£ Ä‘Æ¡n, khÃ¡ch hÃ ng, tour..." />
            </label>
            <label>
                Tráº¡ng thÃ¡i Ä‘Æ¡n
                <select name="bookingStatus" class="form-select">
                    <option value="">Táº¥t cáº£</option>
                    @for (var status = 0; status <= 10; status++)
                    {
                        <option value="@status" selected="@(Model.Filter.BookingStatus == status)">@StatusLabel(status)</option>
                    }
                </select>
            </label>
            <label>
                Tráº¡ng thÃ¡i thanh toÃ¡n
                <select name="paymentStatus" class="form-select">
                    <option value="">Táº¥t cáº£</option>
                    <option value="0" selected="@(Model.Filter.PaymentStatus == 0)">ChÆ°a thanh toÃ¡n</option>
                    <option value="1" selected="@(Model.Filter.PaymentStatus == 1)">Chá» Ä‘á»‘i soÃ¡t</option>
                    <option value="2" selected="@(Model.Filter.PaymentStatus == 2)">ÄÃ£ cá»c</option>
                    <option value="3" selected="@(Model.Filter.PaymentStatus == 3)">ÄÃ£ thanh toÃ¡n Ä‘á»§</option>
                    <option value="4" selected="@(Model.Filter.PaymentStatus == 4)">Thanh toÃ¡n lá»—i</option>
                </select>
            </label>
            <label>
                Tá»« ngÃ y khá»Ÿi hÃ nh
                <input type="date" name="departureFrom" value="@(Model.Filter.DepartureFrom?.ToString("yyyy-MM-dd"))" class="form-control" />
            </label>
            <label>
                Äáº¿n ngÃ y khá»Ÿi hÃ nh
                <input type="date" name="departureTo" value="@(Model.Filter.DepartureTo?.ToString("yyyy-MM-dd"))" class="form-control" />
            </label>
            <div class="hero-actions align-items-end">
                <button type="submit" class="btn btn-primary">Lá»c Ä‘Æ¡n</button>
                <a asp-action="Orders" class="btn btn-outline-secondary">XÃ³a lá»c</a>
            </div>
        </form>

        <div class="table-responsive">
            <table class="table align-middle">
                <thead>
                    <tr>
                        <th>MÃ£ Ä‘Æ¡n</th>
                        <th>KhÃ¡ch hÃ ng</th>
                        <th>Tour</th>
                        <th>Khá»Ÿi hÃ nh</th>
                        <th>KhÃ¡ch</th>
                        <th>Tá»•ng tiá»n</th>
                        <th>Tráº¡ng thÃ¡i</th>
                        <th>Thao tÃ¡c</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Bookings.Count == 0)
                    {
                        <tr>
                            <td colspan="8" class="text-center py-4">KhÃ´ng cÃ³ Ä‘Æ¡n phÃ¹ há»£p vá»›i bá»™ lá»c hiá»‡n táº¡i.</td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var booking in Model.Bookings)
                        {
                            <tr>
                                <td>
                                    <strong>@booking.BookingCode</strong>
                                    <div class="text-muted small">@booking.CreatedAt.ToLocalTime().ToString("dd/MM/yyyy HH:mm")</div>
                                </td>
                                <td>
                                    <strong>@booking.CustomerName</strong>
                                    <div class="text-muted small">@booking.ContactPhone</div>
                                </td>
                                <td>
                                    <strong>@booking.TourName</strong>
                                    <div class="text-muted small">@booking.TourCode</div>
                                </td>
                                <td>@booking.DepartureDate.ToString("dd/MM/yyyy")</td>
                                <td>@booking.Travelers</td>
                                <td>
                                    <strong>@booking.TotalAmount.ToString("N0") Ä‘</strong>
                                    <div class="text-muted small">ÄÃ£ tráº£: @booking.PaidAmount.ToString("N0") Ä‘</div>
                                    <div class="text-muted small">CÃ²n láº¡i: @booking.RemainingAmount.ToString("N0") Ä‘</div>
                                </td>
                                <td>
                                    <div class="d-flex flex-column gap-2">
                                        <span class="status-pill @StatusClass(booking.BookingStatus)">@StatusLabel(booking.BookingStatus)</span>
                                        @if (!string.IsNullOrWhiteSpace(booking.PaymentConfirmedByName))
                                        {
                                            <span class="admin-audit-line">Accountant: <strong>@booking.PaymentConfirmedByName</strong>@(booking.PaymentConfirmedAt.HasValue ? $" â€¢ {booking.PaymentConfirmedAt.Value.ToLocalTime():dd/MM/yyyy HH:mm}" : string.Empty)</span>
                                        }
                                        @if (!string.IsNullOrWhiteSpace(booking.BookingConfirmedByName))
                                        {
                                            <span class="admin-audit-line">Staff: <strong>@booking.BookingConfirmedByName</strong>@(booking.BookingConfirmedAt.HasValue ? $" â€¢ {booking.BookingConfirmedAt.Value.ToLocalTime():dd/MM/yyyy HH:mm}" : string.Empty)</span>
                                        }
                                        @if (!string.IsNullOrWhiteSpace(booking.LatestPaymentCode))
                                        {
                                            <div class="text-muted small">@booking.LatestPaymentCode â€¢ @booking.LatestPaymentAmount.ToString("N0") Ä‘</div>
                                        }
                                        @if (!string.IsNullOrWhiteSpace(booking.LatestPaymentTransactionReference))
                                        {
                                            <div class="text-muted small">MÃ£ GD: @booking.LatestPaymentTransactionReference</div>
                                        }
                                        @if (booking.HasPaymentToVerify && canManageFinance && booking.LatestPaymentId.HasValue)
                                        {
                                            <form asp-action="VerifyPayment" method="post" class="d-flex gap-2">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="paymentId" value="@booking.LatestPaymentId" />
                                                <input type="hidden" name="currentPage" value="@Model.CurrentPage" />
                                                <button type="submit" name="isValid" value="true" class="btn btn-primary btn-sm">XÃ¡c nháº­n tiá»n</button>
                                                <button type="submit" name="isValid" value="false" class="btn btn-outline-danger btn-sm" onclick="return confirm('Tá»« chá»‘i giao dá»‹ch nÃ y?');">Sai giao dá»‹ch</button>
                                            </form>
                                        }
                                        @if (canEditBookingStatus)
                                        {
                                            <form asp-action="UpdateOrderStatus" method="post" class="admin-status-editor">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="BookingId" value="@booking.BookingId" />
                                                <input type="hidden" name="CurrentPage" value="@Model.CurrentPage" />
                                                <select name="BookingStatus" class="form-select form-select-sm">
                                                    @for (var status = 0; status <= 10; status++)
                                                    {
                                                        <option value="@status" selected="@(booking.BookingStatus == status)">@StatusLabel(status)</option>
                                                    }
                                                </select>
                                                <div class="admin-status-editor__actions">
                                                    <input type="text" name="Notes" class="form-control form-control-sm" maxlength="500" placeholder="Ghi chÃº..." />
                                                    <button type="submit" class="btn btn-primary btn-sm">LÆ°u</button>
                                                </div>
                                            </form>
                                            @if (booking.CanStaffProcess && booking.BookingStatus != 3 && booking.BookingStatus != 8)
                                            {
                                                <form asp-action="UpdateOrderStatus" method="post">
                                                    @Html.AntiForgeryToken()
                                                    <input type="hidden" name="BookingId" value="@booking.BookingId" />
                                                    <input type="hidden" name="CurrentPage" value="@Model.CurrentPage" />
                                                    <input type="hidden" name="BookingStatus" value="3" />
                                                    <input type="hidden" name="Notes" value="Staff xác nhận booking sau khi hệ thống ghi nhận thanh toán." />
                                                    <button type="submit" class="btn btn-primary btn-sm">Staff xác nhận booking</button>
                                                </form>
                                            }
                                        }
                                        else if (booking.BookingStatus is 4 or 5)
                                        {
                                            <span class="text-muted small">ÄÆ¡n Ä‘Ã£ Ä‘Ã³ng, khÃ´ng thá»ƒ sá»­a tráº¡ng thÃ¡i.</span>
                                        }
                                        else if (booking.BookingStatus == 9 && canManageBookings)
                                        {
                                            <form asp-action="UpdateOrderStatus" method="post">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="BookingId" value="@booking.BookingId" />
                                                <input type="hidden" name="CurrentPage" value="@Model.CurrentPage" />
                                                <input type="hidden" name="BookingStatus" value="10" />
                                                <button type="submit" class="btn btn-primary btn-sm">Chuyá»ƒn chá» hoÃ n tiá»n</button>
                                            </form>
                                        }
                                        else if (booking.BookingStatus == 9)
                                        {
                                            <span class="text-muted small">Chá» Staff kiá»ƒm tra yÃªu cáº§u hoÃ n tiá»n.</span>
                                        }
                                        else if (booking.BookingStatus == 10 && canManageFinance)
                                        {
                                            <form asp-action="CompleteRefund" method="post" onsubmit="return confirm('XÃ¡c nháº­n Ä‘Ã£ hoÃ n tiá»n cho Ä‘Æ¡n nÃ y?');">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="bookingId" value="@booking.BookingId" />
                                                <input type="hidden" name="currentPage" value="@Model.CurrentPage" />
                                                <button type="submit" class="btn btn-primary btn-sm">ÄÃ£ hoÃ n tiá»n</button>
                                            </form>
                                        }
                                        else if (booking.BookingStatus == 10)
                                        {
                                            <span class="text-muted small">Chá» Accountant hoÃ n tiá»n.</span>
                                        }
                                        else if (!booking.CanStaffProcess)
                                        {
                                            <span class="text-muted small">Chá» Accountant xÃ¡c nháº­n tiá»n.</span>
                                        }
                                        else if (canManageBookings && booking.BookingStatus != 3)
                                        {
                                            <form asp-action="UpdateOrderStatus" method="post">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="BookingId" value="@booking.BookingId" />
                                                <input type="hidden" name="CurrentPage" value="@Model.CurrentPage" />
                                                <input type="hidden" name="BookingStatus" value="3" />
                                                <button type="submit" class="btn btn-primary btn-sm">Staff xÃ¡c nháº­n booking</button>
                                            </form>
                                        }
                                    </div>
                                </td>
                                <td>
                                    @if (canEditBookingStatus)
                                    {
                                        <button type="button"
                                                class="btn btn-primary btn-sm"
                                                data-bs-toggle="modal"
                                                data-bs-target="#editOrderStatusModal"
                                                data-booking-id="@booking.BookingId"
                                                data-booking-code="@booking.BookingCode"
                                                data-booking-status="@booking.BookingStatus">
                                            Sá»­a 
                                        </button>
                                    }
                                    @if (booking.HasPaymentToVerify && canManageFinance && booking.LatestPaymentId.HasValue)
                                    {
                                        <form asp-action="VerifyPayment" method="post" class="admin-row-action-form">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="paymentId" value="@booking.LatestPaymentId" />
                                            <input type="hidden" name="currentPage" value="@Model.CurrentPage" />
                                            <button type="submit" name="isValid" value="true" class="btn btn-success btn-sm">XÃ¡c nháº­n tiá»n</button>
                                            <button type="submit" name="isValid" value="false" class="btn btn-outline-danger btn-sm" onclick="return confirm('Tá»« chá»‘i giao dá»‹ch nÃ y?');">Sai giao dá»‹ch</button>
                                        </form>
                                    }
                                    @if (booking.BookingStatus == 10 && canManageFinance)
                                    {
                                        <form asp-action="CompleteRefund" method="post" class="admin-row-action-form" onsubmit="return confirm('XÃ¡c nháº­n Ä‘Ã£ hoÃ n tiá»n cho Ä‘Æ¡n nÃ y?');">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="bookingId" value="@booking.BookingId" />
                                            <input type="hidden" name="currentPage" value="@Model.CurrentPage" />
                                            <button type="submit" class="btn btn-success btn-sm">ÄÃ£ hoÃ n tiá»n</button>
                                        </form>
                                    }
                                    <button type="button"
                                            class="btn btn-outline-primary btn-sm"
                                            data-bs-toggle="modal"
                                            data-bs-target="#orderDetailModal"
                                            data-booking-code="@booking.BookingCode"
                                            data-customer-name="@booking.CustomerName"
                                            data-contact-name="@booking.ContactName"
                                            data-contact-email="@booking.ContactEmail"
                                            data-contact-phone="@booking.ContactPhone"
                                            data-tour-name="@booking.TourName"
                                            data-tour-code="@booking.TourCode"
                                            data-departure-date="@booking.DepartureDate.ToString("dd/MM/yyyy")"
                                            data-return-date="@booking.ReturnDate.ToString("dd/MM/yyyy")"
                                            data-adult-count="@booking.AdultCount"
                                            data-child-count="@booking.ChildCount"
                                            data-infant-count="@booking.InfantCount"
                                            data-travelers="@booking.Travelers"
                                            data-total-amount="@booking.TotalAmount.ToString("N0") Ä‘"
                                            data-paid-amount="@booking.PaidAmount.ToString("N0") Ä‘"
                                            data-remaining-amount="@booking.RemainingAmount.ToString("N0") Ä‘"
                                            data-booking-status="@StatusLabel(booking.BookingStatus)"
                                            data-payment-code="@booking.LatestPaymentCode"
                                            data-payment-transaction="@booking.LatestPaymentTransactionReference"
                                            data-payment-confirmed-by="@(string.IsNullOrWhiteSpace(booking.PaymentConfirmedByName) ? "ChÆ°a cÃ³ log" : booking.PaymentConfirmedByName)"
                                            data-payment-confirmed-at="@(booking.PaymentConfirmedAt.HasValue ? booking.PaymentConfirmedAt.Value.ToLocalTime().ToString("dd/MM/yyyy HH:mm") : "ChÆ°a cÃ³ log")"
                                            data-booking-confirmed-by="@(string.IsNullOrWhiteSpace(booking.BookingConfirmedByName) ? "ChÆ°a cÃ³ log" : booking.BookingConfirmedByName)"
                                            data-booking-confirmed-at="@(booking.BookingConfirmedAt.HasValue ? booking.BookingConfirmedAt.Value.ToLocalTime().ToString("dd/MM/yyyy HH:mm") : "ChÆ°a cÃ³ log")"
                                            data-special-requests="@(string.IsNullOrWhiteSpace(booking.SpecialRequests) ? "KhÃ´ng cÃ³" : booking.SpecialRequests)">
                                        Chi tiáº¿t
                                    </button>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>

        @if (Model.TotalPages > 1)
        {
            <nav aria-label="PhÃ¢n trang Ä‘Æ¡n">
                <ul class="pagination justify-content-end mb-0">
                    <li class="page-item @(Model.CurrentPage == 1 ? "disabled" : string.Empty)">
                        <a class="page-link" asp-action="Orders" asp-route-page="@(Model.CurrentPage - 1)" asp-route-searchTerm="@Model.Filter.SearchTerm" asp-route-bookingStatus="@Model.Filter.BookingStatus" asp-route-paymentStatus="@Model.Filter.PaymentStatus" asp-route-departureFrom="@(Model.Filter.DepartureFrom?.ToString("yyyy-MM-dd"))" asp-route-departureTo="@(Model.Filter.DepartureTo?.ToString("yyyy-MM-dd"))">TrÆ°á»›c</a>
                    </li>
                    @for (var pageNumber = 1; pageNumber <= Model.TotalPages; pageNumber++)
                    {
                        <li class="page-item @(pageNumber == Model.CurrentPage ? "active" : string.Empty)">
                            <a class="page-link" asp-action="Orders" asp-route-page="@pageNumber" asp-route-searchTerm="@Model.Filter.SearchTerm" asp-route-bookingStatus="@Model.Filter.BookingStatus" asp-route-paymentStatus="@Model.Filter.PaymentStatus" asp-route-departureFrom="@(Model.Filter.DepartureFrom?.ToString("yyyy-MM-dd"))" asp-route-departureTo="@(Model.Filter.DepartureTo?.ToString("yyyy-MM-dd"))">@pageNumber</a>
                        </li>
                    }
                    <li class="page-item @(Model.CurrentPage == Model.TotalPages ? "disabled" : string.Empty)">
                        <a class="page-link" asp-action="Orders" asp-route-page="@(Model.CurrentPage + 1)" asp-route-searchTerm="@Model.Filter.SearchTerm" asp-route-bookingStatus="@Model.Filter.BookingStatus" asp-route-paymentStatus="@Model.Filter.PaymentStatus" asp-route-departureFrom="@(Model.Filter.DepartureFrom?.ToString("yyyy-MM-dd"))" asp-route-departureTo="@(Model.Filter.DepartureTo?.ToString("yyyy-MM-dd"))">Sau</a>
                    </li>
                </ul>
            </nav>
        }
    </section>
</section>

<div class="modal fade" id="editOrderStatusModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <form asp-action="UpdateOrderStatus" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" id="editStatusBookingId" name="BookingId" />
                <input type="hidden" name="CurrentPage" value="@Model.CurrentPage" />
                <div class="modal-header">
                    <h5 class="modal-title">Sá»­a tráº¡ng thÃ¡i Ä‘Æ¡n</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="ÄÃ³ng"></button>
                </div>
                <div class="modal-body">
                    <p id="editStatusBookingCode" class="text-muted mb-3"></p>
                    <label class="w-100">
                        Tráº¡ng thÃ¡i má»›i
                        <select id="editStatusValue" name="BookingStatus" class="form-select">
                            @for (var status = 0; status <= 10; status++)
                            {
                                <option value="@status">@StatusLabel(status)</option>
                            }
                        </select>
                    </label>
                    <label class="w-100 mt-3">
                        Ghi chÃº
                        <textarea name="Notes" class="form-control admin-textarea" maxlength="500" placeholder="Nháº­p lÃ½ do hoáº·c ghi chÃº xá»­ lÃ½..."></textarea>
                    </label>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">ÄÃ³ng</button>
                    <button type="submit" class="btn btn-primary">LÆ°u tráº¡ng thÃ¡i</button>
                </div>
            </form>
        </div>
    </div>
</div>

<div class="modal fade" id="orderDetailModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Chi tiáº¿t Ä‘Æ¡n</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="ÄÃ³ng"></button>
            </div>
            <div class="modal-body">
                <div class="admin-form-grid">
                    <label>
                        MÃ£ Ä‘Æ¡n
                        <input id="detailBookingCode" class="form-control" readonly />
                    </label>
                    <label>
                        Tráº¡ng thÃ¡i
                        <input id="detailBookingStatus" class="form-control" readonly />
                    </label>
                    <label>
                        KhÃ¡ch hÃ ng
                        <input id="detailCustomerName" class="form-control" readonly />
                    </label>
                    <label>
                        NgÆ°á»i liÃªn há»‡
                        <input id="detailContactName" class="form-control" readonly />
                    </label>
                    <label>
                        Email liÃªn há»‡
                        <input id="detailContactEmail" class="form-control" readonly />
                    </label>
                    <label>
                        Sá»‘ Ä‘iá»‡n thoáº¡i
                        <input id="detailContactPhone" class="form-control" readonly />
                    </label>
                    <label>
                        Tour
                        <input id="detailTourName" class="form-control" readonly />
                    </label>
                    <label>
                        MÃ£ tour
                        <input id="detailTourCode" class="form-control" readonly />
                    </label>
                    <label>
                        Khá»Ÿi hÃ nh
                        <input id="detailDepartureDate" class="form-control" readonly />
                    </label>
                    <label>
                        Káº¿t thÃºc
                        <input id="detailReturnDate" class="form-control" readonly />
                    </label>
                    <label>
                        Sá»‘ lÆ°á»£ng khÃ¡ch
                        <input id="detailTravelers" class="form-control" readonly />
                    </label>
                    <label>
                        Tá»•ng tiá»n
                        <input id="detailTotalAmount" class="form-control" readonly />
                    </label>
                    <label>
                        ÄÃ£ thanh toÃ¡n
                        <input id="detailPaidAmount" class="form-control" readonly />
                    </label>
                    <label>
                        CÃ²n láº¡i
                        <input id="detailRemainingAmount" class="form-control" readonly />
                    </label>
                    <label>
                        MÃ£ thanh toÃ¡n
                        <input id="detailPaymentCode" class="form-control" readonly />
                    </label>
                    <label>
                        MÃ£ giao dá»‹ch VNPay
                        <input id="detailPaymentTransaction" class="form-control" readonly />
                    </label>
                    <label>
                        Accountant xÃ¡c nháº­n
                        <input id="detailPaymentConfirmedBy" class="form-control" readonly />
                    </label>
                    <label>
                        Thá»i Ä‘iá»ƒm xÃ¡c nháº­n tiá»n
                        <input id="detailPaymentConfirmedAt" class="form-control" readonly />
                    </label>
                    <label>
                        Staff xÃ¡c nháº­n booking
                        <input id="detailBookingConfirmedBy" class="form-control" readonly />
                    </label>
                    <label>
                        Thá»i Ä‘iá»ƒm xÃ¡c nháº­n booking
                        <input id="detailBookingConfirmedAt" class="form-control" readonly />
                    </label>
                </div>

                <label class="mt-3">
                    YÃªu cáº§u Ä‘áº·c biá»‡t
                    <textarea id="detailSpecialRequests" class="form-control admin-textarea" readonly></textarea>
                </label>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">ÄÃ³ng</button>
            </div>
        </div>
    </div>
</div>

<script>
    (() => {
        const modal = document.getElementById('editOrderStatusModal');
        if (!modal) {
            return;
        }

        modal.addEventListener('show.bs.modal', event => {
            const button = event.relatedTarget;
            if (!button) {
                return;
            }

            document.getElementById('editStatusBookingId').value = button.getAttribute('data-booking-id') || '';
            document.getElementById('editStatusValue').value = button.getAttribute('data-booking-status') || '0';
            document.getElementById('editStatusBookingCode').textContent = `ÄÆ¡n ${button.getAttribute('data-booking-code') || ''}`;
        });
    })();

    (() => {
        const modal = document.getElementById('orderDetailModal');
        if (!modal) {
            return;
        }

        modal.addEventListener('show.bs.modal', event => {
            const button = event.relatedTarget;
            if (!button) {
                return;
            }

            const travelers = `${button.getAttribute('data-travelers')} khÃ¡ch (${button.getAttribute('data-adult-count')} NL, ${button.getAttribute('data-child-count')} TE, ${button.getAttribute('data-infant-count')} EB)`;

            document.getElementById('detailBookingCode').value = button.getAttribute('data-booking-code') || '';
            document.getElementById('detailBookingStatus').value = button.getAttribute('data-booking-status') || '';
            document.getElementById('detailCustomerName').value = button.getAttribute('data-customer-name') || '';
            document.getElementById('detailContactName').value = button.getAttribute('data-contact-name') || '';
            document.getElementById('detailContactEmail').value = button.getAttribute('data-contact-email') || '';
            document.getElementById('detailContactPhone').value = button.getAttribute('data-contact-phone') || '';
            document.getElementById('detailTourName').value = button.getAttribute('data-tour-name') || '';
            document.getElementById('detailTourCode').value = button.getAttribute('data-tour-code') || '';
            document.getElementById('detailDepartureDate').value = button.getAttribute('data-departure-date') || '';
            document.getElementById('detailReturnDate').value = button.getAttribute('data-return-date') || '';
            document.getElementById('detailTravelers').value = travelers;
            document.getElementById('detailTotalAmount').value = button.getAttribute('data-total-amount') || '';
            document.getElementById('detailPaidAmount').value = button.getAttribute('data-paid-amount') || '';
            document.getElementById('detailRemainingAmount').value = button.getAttribute('data-remaining-amount') || '';
            document.getElementById('detailPaymentCode').value = button.getAttribute('data-payment-code') || '';
            document.getElementById('detailPaymentTransaction').value = button.getAttribute('data-payment-transaction') || '';
            document.getElementById('detailPaymentConfirmedBy').value = button.getAttribute('data-payment-confirmed-by') || '';
            document.getElementById('detailPaymentConfirmedAt').value = button.getAttribute('data-payment-confirmed-at') || '';
            document.getElementById('detailBookingConfirmedBy').value = button.getAttribute('data-booking-confirmed-by') || '';
            document.getElementById('detailBookingConfirmedAt').value = button.getAttribute('data-booking-confirmed-at') || '';
            document.getElementById('detailSpecialRequests').value = button.getAttribute('data-special-requests') || '';
        });
    })();
</script>

@functions {
    private static string StatusLabel(int status) => status switch
    {
        0 => "Chá» thanh toÃ¡n",
        1 => "Chá» xÃ¡c nháº­n thanh toÃ¡n",
        2 => "ÄÃ£ cá»c",
        3 => "ÄÃ£ xÃ¡c nháº­n booking",
        4 => "ÄÃ£ há»§y",
        5 => "ÄÃ£ hoÃ n tiá»n",
        6 => "Chá» thanh toÃ¡n cÃ²n láº¡i",
        7 => "Chá» xÃ¡c nháº­n thanh toÃ¡n Ä‘á»§",
        8 => "ÄÃ£ thanh toÃ¡n",
        9 => "YÃªu cáº§u hoÃ n tiá»n",
        10 => "Chá» hoÃ n tiá»n",
        _ => "KhÃ´ng xÃ¡c Ä‘á»‹nh"
    };

    private static string StatusClass(int status) => status switch
    {
        2 or 3 or 8 => "status-pill--success",
        4 or 5 => "status-pill--danger",
        1 or 6 or 7 or 9 or 10 => "status-pill--info",
        _ => "status-pill--warning"
    };
}
