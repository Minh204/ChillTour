@model ChillTour.Models.Admin.AdminPromotionListViewModel
@{
    ViewData["Title"] = "Quản lý ưu đãi";
}

<section class="admin-page-stack">
    <div class="admin-page-head">
        <div>
            <span class="admin-panel__label">Promotion</span>
            <h2>Quản lý ưu đãi</h2>
            <p>Tạo mã ưu đãi, theo dõi hạn dùng và cấu hình banner hiển thị ở trang chủ.</p>
        </div>
        <a asp-action="CreatePromotion" class="btn btn-primary nav-cta">Thêm ưu đãi</a>
    </div>

    @if (TempData["AdminSuccessMessage"] is string successMessage)
    {
        <div class="auth-alert auth-alert--success">@successMessage</div>
    }
    @if (TempData["AdminErrorMessage"] is string errorMessage)
    {
        <div class="auth-alert auth-alert--error">@errorMessage</div>
    }

    <div class="admin-overview-grid">
        <article class="admin-stat-card">
            <span class="admin-panel__label">Total</span>
            <strong>@Model.TotalPromotions</strong>
            <p>Tổng số ưu đãi</p>
        </article>
        <article class="admin-stat-card admin-mini-card--green">
            <span class="admin-panel__label">Live</span>
            <strong>@Model.ActivePromotions</strong>
            <p>Đang còn hiệu lực</p>
        </article>
        <article class="admin-stat-card admin-mini-card--orange">
            <span class="admin-panel__label">Expired</span>
            <strong>@Model.ExpiredPromotions</strong>
            <p>Đã hết hạn hoặc ngừng áp dụng</p>
        </article>
    </div>

    <section class="admin-panel">
        <form method="get" class="admin-form-grid app-filter-row mb-4">
            <label>
                Từ khóa
                <input type="text" name="searchTerm" value="@Model.Filter.SearchTerm" class="form-control" placeholder="Mã hoặc tên ưu đãi..." />
            </label>
            <label>
                Trạng thái
                <select name="status" class="form-select">
                    <option value="">Tất cả</option>
                    <option value="live" selected="@(Model.Filter.Status == "live")">Còn hiệu lực</option>
                    <option value="expired" selected="@(Model.Filter.Status == "expired")">Hết hiệu lực</option>
                </select>
            </label>
            <label>
                Cơ chế áp dụng
                <select name="isAutoApply" class="form-select">
                    <option value="">Tất cả</option>
                    <option value="true" selected="@(Model.Filter.IsAutoApply == true)">Tự áp dụng</option>
                    <option value="false" selected="@(Model.Filter.IsAutoApply == false)">Nhập thủ công</option>
                </select>
            </label>
            <div class="hero-actions align-items-end">
                <button type="submit" class="btn btn-primary">Lọc ưu đãi</button>
                <a asp-action="Promotions" class="btn btn-outline-secondary">Xóa lọc</a>
            </div>
        </form>

        <div class="admin-table-wrap">
            <table class="admin-table admin-promotion-table">
                <thead>
                    <tr>
                        <th>Banner</th>
                        <th>Mã</th>
                        <th>Sự kiện ưu đãi</th>
                        <th>% giảm</th>
                        <th>Hạn dùng</th>
                        <th>Đã nhận</th>
                        <th>Đã dùng</th>
                        <th>Trạng thái</th>
                        <th>Hành động</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Promotions.Count == 0)
                    {
                        <tr>
                            <td colspan="9" class="text-center py-4">Không có ưu đãi phù hợp với bộ lọc hiện tại.</td>
                        </tr>
                    }
                    else
                    {
                        @foreach (var item in Model.Promotions)
                        {
                            var isLive = item.IsActive && item.EndAt >= DateTime.UtcNow;
                            <tr>
                                <td>
                                    @if (!string.IsNullOrWhiteSpace(item.BannerImageUrl))
                                    {
                                        <div class="admin-promotion-banner-thumb">
                                            <img src="@item.BannerImageUrl" alt="@item.PromotionName" />
                                            @if (item.ShowOnHomeBanner)
                                            {
                                                <span>Home</span>
                                            }
                                        </div>
                                    }
                                    else
                                    {
                                        <span class="admin-table__sub">Chưa có</span>
                                    }
                                </td>
                                <td><strong>@item.PromotionCode</strong></td>
                                <td>
                                    @item.PromotionName
                                    @if (item.ShowOnHomeBanner)
                                    {
                                        <div class="admin-table__sub">Banner thứ tự @item.BannerDisplayOrder</div>
                                    }
                                </td>
                                <td>@(item.DiscountPercent.HasValue ? $"{item.DiscountPercent.Value:N0}%" : "-")</td>
                                <td>@item.EndAt.ToLocalTime().ToString("dd/MM/yyyy HH:mm")</td>
                                <td>@item.ClaimedCount</td>
                                <td>@item.UsedCount</td>
                                <td>
                                    <span class="status-pill @(isLive ? "status-pill--success" : "status-pill--danger")">
                                        @(isLive ? "Còn hiệu lực" : "Hết hiệu lực")
                                    </span>
                                </td>
                                <td>
                                    <div class="d-flex gap-2">
                                        <a asp-action="EditPromotion" asp-route-id="@item.PromotionId" class="btn btn-outline-primary btn-sm">Sửa</a>
                                        <form asp-action="DeletePromotion" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="id" value="@item.PromotionId" />
                                            <button type="submit" class="btn btn-outline-danger btn-sm">Xóa</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </section>
</section>
